# Deploy / Ops Run Record (2026-04-19)

## Result
- Status: ✅ Completed (ready for production operation)

## What was verified
- Secrets / env vars reviewed (Vercel + local Vite env; no secrets committed)
- Supabase:
  - Admin profile: `role=admin`, `is_active=true`
  - RLS enabled on required tables
  - Policies present for admin + learner flows
  - Grants limited for `authenticated` (no broad `anon` grants)
  - `invite_api_request_logs.action` CHECK includes `accept`
- Resend:
  - Domain `ai-nagoya.com` is verified
  - Invitation email send logged as `success`
- Frontend (Xserver):
  - SPA routing works (direct `/admin/users` does not 404)
- End-to-end:
  - Invite `create` → accept → watch → logs updated (`create` + `accept`)

## Artifacts
- Audit CSV exports saved locally (rename per convention):
  - `invite_email_logs_2026-04-19.csv`
  - `invite_api_request_logs_2026-04-19.csv`
- Storage: move these to the admin-only shared drive folder used by your org.

